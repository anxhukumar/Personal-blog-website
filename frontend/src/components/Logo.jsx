import React from 'react'

function Logo({className}) {
  return (
    <div>
        <h2 className={`transition duration-700 font-bold text-3xl text-center ${className}`}>
          anxhukumar
        </h2>
    </div>
  )
}

export default Logo